# Dependencies
node_modules/

# Build output
dist/
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.*
!.env.example

# TypeScript / Vite caches
*.tsbuildinfo
.vite/

# OS and editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local build export bundle (source + node_modules + optional base images,
# generated on demand)
/build-export/

# Per-network values overlay for the export bundle (copy from
# chart/values.closed-network.yaml.example) — network-specific, not shared
/chart/values.closed-network.yaml

