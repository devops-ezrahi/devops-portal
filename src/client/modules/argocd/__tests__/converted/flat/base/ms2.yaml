# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Shared, environment-agnostic base for 'ms2' — reused by every namespace
# that runs this microservice. Deliberately excludes image tags and env vars;
# those live in each namespace's own values/<namespace>/ms2.yaml.
# Re-run the converter to regenerate after the source manifests change.

workload:
  type: deployment
containerName: app
env:
  DB_URL:
    value: http://db.{{ .Values.environment }}.svc:5432
  ENVIRONMENT:
    value: '{{ .Values.environment }}'
image:
  repository: registry.example.org/ms2
nameOverride: ms2
service:
  enabled: true
  name: ms2
  ports:
    http:
      port: 8080
      targetPort: 8080
  type: ClusterIP
