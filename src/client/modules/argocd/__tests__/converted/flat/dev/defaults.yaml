# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Namespace defaults for 'dev': values identical across every
# release in THIS folder, the shared release included, plus the group
# values (--env-group / --env-suffixes) the folder stands for. Applied
# over base/<release>.yaml, beneath this folder's values/<release>.yaml.
# Re-run the converter to regenerate after the source manifests change.

workload:
  type: deployment
containerName: app
environment: dev
service:
  enabled: true
  ports:
    http:
      port: 8080
      targetPort: 8080
  type: ClusterIP
