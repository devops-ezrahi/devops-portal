# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# One Application per NAMESPACE DIRECTORY in this tree, each deploying the
# 'ms-applicationSet' chart, which in turn fans out over that
# namespace's values/*.yaml. A new namespace is a new directory here and
# nothing else — no manifest to add, no apply to run.
# Deployed by rootApplication.yaml.txt; do not apply it by hand.

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  annotations:
    generated-by: convert_to_universal_chart.py
  labels:
    app: example-flat
  name: example-flat
  namespace: argocd
spec:
  generators:
  - git:
      directories:
      - path: '*'
      - exclude: true
        path: base
      - exclude: true
        path: cluster-shared
      - exclude: true
        path: ERRORS_ANALYSIS
      - exclude: true
        path: report
      repoURL: https://github.com/devops-ezrahi/universal-chart-example-flat.git
      revision: main
  template:
    metadata:
      labels:
        app: example-flat
        managed-by: universal-chart-converter
      name: '{{path.basename}}'
    spec:
      destination:
        namespace: argocd
        server: https://kubernetes.default.svc
      project: default
      sources:
      - helm:
          parameters:
          - name: namespace
            value: '{{path.basename}}'
          - name: originRepoURL
            value: https://github.com/devops-ezrahi/universal-chart-example-flat.git
          - name: originBranch
            value: main
          - name: originPath
            value: ''
          - name: project
            value: default
          - name: destinationServer
            value: https://kubernetes.default.svc
          - name: chartRepoURL
            value: https://github.com/devops-ezrahi/universal-chart.git
          - name: chartRevision
            value: main
          - name: chartPath
            value: .
        path: ms-applicationSet
        repoURL: https://github.com/devops-ezrahi/universal-chart.git
        targetRevision: main
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - ServerSideApply=true
