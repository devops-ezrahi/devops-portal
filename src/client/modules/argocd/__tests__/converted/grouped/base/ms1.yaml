# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Shared, environment-agnostic base for 'ms1' — reused by every namespace
# that runs this microservice. Deliberately excludes image tags and env vars;
# those live in each namespace's own values/<namespace>/ms1.yaml.
# Re-run the converter to regenerate after the source manifests change.

workload:
  type: deployment
containerName: app
env:
  COLOR:
    value: '{{ .Values.color }}'
  DB_URL:
    value: http://db.{{ .Values.environment }}.svc:5432
  ENVIRONMENT:
    value: '{{ .Values.environment }}'
fullnameOverride: ms1-{{ .Values.color }}
image:
  repository: registry.example.org/ms1
nameOverride: ms1
service:
  enabled: true
  name: ms1-{{ .Values.color }}
  ports:
    http:
      port: 8080
      targetPort: 8080
  type: ClusterIP
