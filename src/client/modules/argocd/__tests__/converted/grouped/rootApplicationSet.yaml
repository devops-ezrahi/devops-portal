# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# One Application per NAMESPACE DIRECTORY in this tree, each deploying the
# 'ms-applicationSet' chart, which in turn fans out over that
# namespace's values/*.yaml. A new namespace is a new directory here and
# nothing else — no manifest to add, no apply to run.
# Deployed by rootApplication.yaml.txt; do not apply it by hand.

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  annotations:
    generated-by: convert_to_universal_chart.py
  labels:
    app: platform-root-set
  name: platform-root-set
  namespace: argocd
spec:
  generators:
  - git:
      files:
      - path: '**/defaults.yaml'
      repoURL: https://github.com/devops-ezrahi/example-values.git
      revision: main
  goTemplate: true
  goTemplateOptions:
  - missingkey=error
  template:
    metadata:
      labels:
        app: platform-root-set
        managed-by: universal-chart-converter
      name: '{{ .path.path | replace "/" "-" }}'
    spec:
      destination:
        namespace: argocd
        server: https://kubernetes.default.svc
      project: default
      sources:
      - helm:
          parameters:
          - name: namespace
            value: '{{ index .path.segments 0 }}'
          - name: folder
            value: '{{ .path.path }}'
          - name: originRepoURL
            value: https://github.com/devops-ezrahi/example-values.git
          - name: originBranch
            value: main
          - name: originPath
            value: ''
          - name: project
            value: default
          - name: destinationServer
            value: https://kubernetes.default.svc
          - name: chartRepoURL
            value: https://github.com/devops-ezrahi/universal-chart.git
          - name: chartRevision
            value: main
          - name: chartPath
            value: .
        path: ms-applicationSet
        repoURL: https://github.com/devops-ezrahi/universal-chart.git
        targetRevision: main
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - ServerSideApply=true
